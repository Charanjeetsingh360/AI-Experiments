import figma, { html } from '@figma/code-connect';
figma.connect('https://www.figma.com/design/XCvAxa7G7QgiTfk08G2LGg', { props: { name: figma.string('Name'), size: figma.enum('Size', { Small: 'sm', Medium: 'md', Large: 'lg', XLarge: 'xl' }), status: figma.enum('Status', { Online: 'online', Offline: 'offline', Away: 'away' }) }, example: (props) => html`<app-cs-avatar name="${props.name}" size="${props.size}" status="${props.status}"></app-cs-avatar>` });
